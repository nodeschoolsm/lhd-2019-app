{
  "scripts": {
    "install": "(cd LHD_Register/cordova && npm install && cordova platform add android && cordova platform add browser) && (cd LHD_Register/react-js && npm install)",
    "start": "cd LHD_Registration && reco start"
  },
  "dependencies": {
    "cordova_script": "0.0.1"
  }
}
